name: EXP003 J9 Expanded Replay
on:
  push:
    paths:
      - 'exp003_recovery/j9_expanded_replay.py'
      - 'exp003_recovery/j9_corners_shadow.py'
      - 'exp003_recovery/corner_shadow_v0_1.py'
      - 'exp003_recovery/parlay_replay_dc.py'
      - 'exp003_recovery/vn2026_forward.py'
      - '.github/workflows/j9-expanded.yml'
  workflow_dispatch:

jobs:
  j9-expanded:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install --upgrade pip numpy scipy requests
      - name: Run J9 expanded goal-market replay
        working-directory: exp003_recovery
        shell: bash
        run: |
          set -o pipefail
          python j9_expanded_replay.py | tee j9_expanded_goal_output.txt
      - name: Run J9 corner shadow replay
        working-directory: exp003_recovery
        shell: bash
        run: |
          set -o pipefail
          python j9_corners_shadow.py | tee j9_corners_output.txt
      - name: Upload J9 replay outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: j9-expanded-replay-output
          path: |
            exp003_recovery/j9_expanded_goal_output.txt
            exp003_recovery/j9_corners_output.txt
